# 📧 Настройка Email для подтверждения регистрации

## Обзор

Для отправки email с кодами подтверждения регистрации используется библиотека `nodemailer`. В режиме разработки можно использовать тестовый сервис Ethereal Email, а для продакшена необходимо настроить реальный SMTP сервер.

## 🔧 Настройка для продакшена

### Вариант 1: Gmail (рекомендуется для начала)

Gmail - самый простой вариант для начала работы с отправкой email.

#### Шаг 1: Создание пароля приложения в Gmail

1. Перейдите в [Настройки аккаунта Google](https://myaccount.google.com/)
2. Включите **Двухфакторную аутентификацию** (если еще не включена)
3. Перейдите в раздел **Безопасность** → **Пароли приложений**
4. Выберите приложение: **Почта**
5. Выберите устройство: **Другое (указать название)**
6. Введите название: `CashStream Backend`
7. Нажмите **Создать**
8. **Скопируйте 16-значный пароль** (он показывается только один раз!)

#### Шаг 2: Настройка переменных окружения

Добавьте следующие переменные в файл `.env` в корне проекта `backendCashStream`:

```env
# Email настройки для продакшена
NODE_ENV=production
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-digit-app-password
SMTP_FROM=noreply@cashstream.com
FRONTEND_URL=https://your-app-url.com
```

**Важно:**
- `SMTP_USER` - ваш Gmail адрес (например: `myapp@gmail.com`)
- `SMTP_PASS` - 16-значный пароль приложения, созданный в шаге 1
- `SMTP_FROM` - адрес отправителя (может быть тот же Gmail или другой)
- `FRONTEND_URL` - URL вашего фронтенда (для ссылок в письмах)

#### Шаг 3: Проверка настройки

После настройки перезапустите сервер:

```bash
cd backendCashStream
yarn dev
```

Попробуйте зарегистрировать нового пользователя. Проверьте логи сервера - должны быть сообщения об успешной отправке email.

---

### Вариант 2: AWS SES (лучший для продакшена)

AWS SES (Simple Email Service) - самый экономичный и масштабируемый вариант для продакшена.

#### Шаг 1: Настройка AWS SES

1. Зарегистрируйтесь на [AWS](https://aws.amazon.com/)
2. Перейдите в сервис **SES** (Simple Email Service)
3. Выберите регион (например, `us-east-1` или `eu-west-1`)
4. Верифицируйте email или домен:
   - Для тестирования: **Verify a New Email Address**
   - Для продакшена: **Verify a New Domain** (рекомендуется)
5. После верификации запросите выход из sandbox (если нужно отправлять на любые адреса)

#### Шаг 2: Создание SMTP credentials

1. В SES перейдите в **SMTP Settings**
2. Нажмите **Create SMTP Credentials**
3. Введите имя (например: `CashStream`)
4. **Скопируйте SMTP username и password** (показываются только один раз!)

#### Шаг 3: Настройка переменных окружения

```env
NODE_ENV=production
SMTP_HOST=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@yourdomain.com
FRONTEND_URL=https://your-app-url.com
```

**Важно:**
- `SMTP_HOST` зависит от региона AWS (например: `email-smtp.us-east-1.amazonaws.com`)
- `SMTP_USER` и `SMTP_PASS` - это SMTP credentials из AWS SES
- `SMTP_FROM` - должен быть верифицированный email или домен

**Регионы и SMTP endpoints:**
- `us-east-1`: `email-smtp.us-east-1.amazonaws.com`
- `us-west-2`: `email-smtp.us-west-2.amazonaws.com`
- `eu-west-1`: `email-smtp.eu-west-1.amazonaws.com`
- `ap-southeast-1`: `email-smtp.ap-southeast-1.amazonaws.com`

**Цены:**
- Первые 62,000 писем/месяц бесплатно (если запущено на EC2)
- После: $0.10 за 1000 писем
- Очень дешево для больших объемов!

---

### Вариант 3: SendGrid (простой для старта)

SendGrid - профессиональный сервис для отправки транзакционных email с хорошей доставляемостью.

#### Шаг 1: Регистрация в SendGrid

1. Зарегистрируйтесь на [SendGrid](https://sendgrid.com/)
2. Подтвердите email и завершите регистрацию
3. Перейдите в **Settings** → **API Keys**
4. Нажмите **Create API Key**
5. Дайте имя ключу (например: `CashStream Production`)
6. Выберите права: **Full Access** или **Restricted Access** (только отправка email)
7. **Скопируйте API ключ** (показывается только один раз!)

#### Шаг 2: Настройка переменных окружения

```env
NODE_ENV=production
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
SMTP_FROM=noreply@yourdomain.com
FRONTEND_URL=https://your-app-url.com
```

**Важно:**
- `SMTP_USER` - всегда `apikey` для SendGrid
- `SMTP_PASS` - ваш API ключ из SendGrid
- `SMTP_FROM` - должен быть верифицированный домен или email в SendGrid

#### Шаг 3: Верификация отправителя

В SendGrid необходимо верифицировать адрес отправителя:
1. Перейдите в **Settings** → **Sender Authentication**
2. Выберите **Verify a Single Sender**
3. Заполните форму и подтвердите email

---

### Вариант 4: Mailgun

Mailgun - еще один популярный сервис для отправки email.

#### Шаг 1: Регистрация в Mailgun

1. Зарегистрируйтесь на [Mailgun](https://www.mailgun.com/)
2. Подтвердите email и добавьте домен (или используйте sandbox домен для тестирования)
3. Перейдите в **Sending** → **Domain Settings**
4. Найдите **SMTP credentials**
5. **Скопируйте SMTP username и password**

#### Шаг 2: Настройка переменных окружения

```env
NODE_ENV=production
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=postmaster@your-domain.mailgun.org
SMTP_PASS=your-mailgun-smtp-password
SMTP_FROM=noreply@yourdomain.com
FRONTEND_URL=https://your-app-url.com
```

---

### Вариант 5: Yandex SMTP (для России/СНГ)

Yandex - хороший вариант для проектов с аудиторией в России и СНГ.

#### Шаг 1: Настройка Yandex

1. Зарегистрируйтесь на [Yandex](https://yandex.ru/)
2. Перейдите в **Настройки** → **Пароли и авторизация**
3. Включите **Пароли приложений**
4. Создайте пароль приложения для "Почта"
5. **Скопируйте пароль приложения**

#### Шаг 2: Настройка переменных окружения

```env
NODE_ENV=production
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-app-password
SMTP_FROM=your-email@yandex.ru
FRONTEND_URL=https://your-app-url.com
```

**Важно:**
- Используйте пароль приложения, а не основной пароль
- Порт 465 с `SMTP_SECURE=true` для SSL
- Лимит: до 500 писем в день

---

### Вариант 6: Собственный SMTP сервер

Если у вас есть собственный SMTP сервер (например, на хостинге):

```env
NODE_ENV=production
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@yourdomain.com
FRONTEND_URL=https://your-app-url.com
```

Или для SSL/TLS:

```env
SMTP_PORT=465
SMTP_SECURE=true
```

---

## 🧪 Настройка для разработки (Ethereal Email)

Для разработки можно использовать бесплатный тестовый сервис Ethereal Email. Он автоматически создает тестовые аккаунты и показывает письма в браузере.

### Автоматическая настройка (рекомендуется)

Просто не устанавливайте переменные `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS` в `.env`, и система автоматически использует Ethereal Email.

### Ручная настройка

1. Перейдите на [Ethereal Email](https://ethereal.email/)
2. Нажмите **Create Account**
3. Скопируйте данные из созданного аккаунта

```env
NODE_ENV=development
ETHEREAL_USER=your-ethereal-username
ETHEREAL_PASS=your-ethereal-password
```

**Важно:** В режиме разработки код также будет выводиться в консоль сервера для удобства тестирования.

---

## 📝 Пример полного `.env` файла

```env
# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=cashstream

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Email (для продакшена - раскомментируйте нужный вариант)
NODE_ENV=production

# Вариант 1: Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-digit-app-password
SMTP_FROM=noreply@cashstream.com

# Вариант 2: SendGrid (раскомментируйте, если используете)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=noreply@yourdomain.com

# Frontend URL (для ссылок в письмах)
FRONTEND_URL=https://your-app-url.com
```

---

## 🔍 Проверка работы

### 1. Проверка отправки email

После настройки попробуйте зарегистрировать нового пользователя. В логах сервера вы должны увидеть:

```
✅ Verification email sent: <message-id>
```

### 2. Проверка получения email

- Проверьте папку **Входящие** (и **Спам**, если письмо не пришло)
- В режиме разработки с Ethereal Email проверьте ссылку в консоли:
  ```
  📧 Preview URL: https://ethereal.email/message/...
  ```

### 3. Проверка кода подтверждения

- Код должен быть 6-значным числом
- Код действителен 10 минут
- После использования код становится недействительным

---

## ⚠️ Важные замечания

1. **Безопасность паролей:**
   - Никогда не коммитьте `.env` файл в Git
   - Используйте разные пароли для разработки и продакшена
   - Регулярно обновляйте пароли приложений

2. **Лимиты отправки:**
   - Gmail: до 500 писем в день (бесплатный аккаунт)
   - SendGrid: до 100 писем в день (бесплатный план)
   - Mailgun: до 5000 писем в месяц (бесплатный план)

3. **Доставляемость:**
   - Используйте верифицированные домены для лучшей доставляемости
   - Настройте SPF, DKIM и DMARC записи для вашего домена
   - Избегайте спам-триггеров в тексте писем

4. **Мониторинг:**
   - Настройте логирование отправленных писем
   - Отслеживайте ошибки доставки
   - Мониторьте использование квот

---

## 🐛 Решение проблем

### Проблема: "Invalid login"

**Решение:**
- Проверьте правильность `SMTP_USER` и `SMTP_PASS`
- Для Gmail убедитесь, что используете пароль приложения, а не обычный пароль
- Проверьте, что двухфакторная аутентификация включена (для Gmail)

### Проблема: "Connection timeout"

**Решение:**
- Проверьте, что порт не заблокирован файрволом
- Попробуйте использовать порт 465 с `SMTP_SECURE=true`
- Проверьте правильность `SMTP_HOST`

### Проблема: Письма попадают в спам

**Решение:**
- Используйте верифицированный домен
- Настройте SPF, DKIM записи
- Избегайте спам-слов в теме и тексте письма
- Используйте профессиональные сервисы (SendGrid, Mailgun)

### Проблема: Письма не отправляются

**Решение:**
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что все переменные окружения установлены
- Проверьте подключение к интернету
- В режиме разработки проверьте ссылку Preview URL в консоли

---

## 📚 Дополнительные ресурсы

- [Nodemailer документация](https://nodemailer.com/about/)
- [Gmail пароли приложений](https://support.google.com/accounts/answer/185833)
- [SendGrid документация](https://docs.sendgrid.com/)
- [Mailgun документация](https://documentation.mailgun.com/)

---

## ✅ Чеклист настройки

- [ ] Выбран и настроен SMTP провайдер
- [ ] Переменные окружения добавлены в `.env`
- [ ] `.env` добавлен в `.gitignore`
- [ ] Сервер перезапущен после изменений
- [ ] Тестовая регистрация выполнена успешно
- [ ] Email получен и код работает
- [ ] Настроены SPF/DKIM записи (для продакшена)
- [ ] Настроен мониторинг отправки email

