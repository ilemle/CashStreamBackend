# CashStream Backend API

Backend на Express.js + TypeScript для приложения CashStream.

## 🚀 Начало работы

### Требования
- Node.js (v18 или выше)
- MySQL 8.0+
- yarn или npm

### Установка

1. Установите зависимости:
```bash
yarn install
```

2. Настройте переменные окружения в `.env`:
```bash
PORT=3000
NODE_ENV=development
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-password
DB_NAME=cashstream
JWT_SECRET=your-secret-key-change-this-in-production
```

3. Примените миграции:
```bash
yarn migrate:all
```

4. Запустите development сервер:
```bash
yarn dev
```

5. Или запустите production сервер:
```bash
yarn build
yarn start
```

## 📝 Доступные команды

- `yarn dev` - Запустить development сервер с nodemon
- `yarn start` - Запустить production сервер
- `yarn build` - Собрать TypeScript проект
- `yarn migrate` - Применить миграцию
- `yarn migrate:all` - Применить все миграции
- `yarn generate:operations` - Сгенерировать тестовые операции

## 🏗️ Архитектура

Проект построен по принципу **многослойной архитектуры**:

- **Routes** - Маршрутизация и валидация
- **Controllers** - Обработка HTTP запросов
- **Models** - Работа с базой данных
- **Services** - Бизнес-логика и интеграции

📖 **Подробная документация**: [ARCHITECTURE.md](./ARCHITECTURE.md)

## 📊 Типы данных

Все типы данных документированы:

- **Типы БД**: `src/types/database.ts` - Структура таблиц
- **Типы API**: `src/types/api.ts` - Запросы и ответы API
- **DTO**: `src/types/dto.ts` - Объекты передачи данных

📖 **Схема БД**: [DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md)

## 🔗 API Endpoints

📖 **Полная документация**: [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

### Основные эндпоинты:

- `GET /` - Информация об API
- `GET /health` - Проверка работоспособности

### Аутентификация (`/api/auth`)
- `POST /api/auth/register/send-code` - Отправить код верификации
- `POST /api/auth/register/verify` - Верифицировать и зарегистрировать
- `POST /api/auth/login` - Войти
- `GET /api/auth/me` - Получить текущего пользователя

### Операции (`/api/operations`)
- `GET /api/operations` - Список операций (с пагинацией)
- `POST /api/operations` - Создать операцию
- `GET /api/operations/:id` - Получить операцию
- `PUT /api/operations/:id` - Обновить операцию
- `DELETE /api/operations/:id` - Удалить операцию
- `GET /api/operations/balance` - Получить баланс
- `POST /api/operations/batch` - Создать несколько операций

### Бюджеты (`/api/budgets`)
- `GET /api/budgets` - Список бюджетов
- `POST /api/budgets` - Создать бюджет
- `PUT /api/budgets/:id` - Обновить бюджет
- `DELETE /api/budgets/:id` - Удалить бюджет

### Цели (`/api/goals`)
- `GET /api/goals` - Список целей
- `POST /api/goals` - Создать цель
- `PUT /api/goals/:id` - Обновить цель
- `DELETE /api/goals/:id` - Удалить цель

### Категории (`/api/categories`)
- `GET /api/categories` - Список категорий
- `POST /api/categories` - Создать категорию
- `POST /api/categories/:id/subcategories` - Добавить подкатегорию

### Долги (`/api/debts`)
- `GET /api/debts` - Список долгов
- `POST /api/debts` - Создать долг
- `GET /api/debts/overdue` - Просроченные долги

## 🛠️ Стек технологий

- **Express.js** - веб-фреймворк
- **TypeScript** - типизация
- **MySQL 8.0** - база данных
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей
- **CORS** - разрешение кросс-доменных запросов
- **Morgan** - логирование запросов
- **dotenv** - управление переменными окружения
- **nodemon** - автоматическая перезагрузка при разработке

## 📁 Структура проекта

```
src/
├── config/         # Конфигурация (БД)
├── constants/      # Константы (категории)
├── controllers/    # Контроллеры (обработчики запросов)
├── middleware/     # Middleware (auth, errorHandler, currency)
├── models/         # Модели (работа с БД)
├── routes/         # Маршруты API
├── services/       # Сервисы (Telegram, Email, SMS)
├── types/          # TypeScript типы (БД, API, DTO)
└── utils/         # Утилиты (валюты, форматирование)
```

## 📚 Документация

- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - Архитектура приложения
- **[DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md)** - Схема базы данных
- **[API_DOCUMENTATION.md](./API_DOCUMENTATION.md)** - Документация API
- **[README_ARCHITECTURE.md](./README_ARCHITECTURE.md)** - Краткое описание архитектуры

## 🔧 Разработка

### Добавление нового функционала

1. Создать типы в `src/types/`
2. Создать модель в `src/models/`
3. Создать контроллер в `src/controllers/`
4. Создать роуты в `src/routes/`
5. Зарегистрировать в `src/index.ts`

Подробнее см. [ARCHITECTURE.md](./ARCHITECTURE.md#расширение-проекта)

## 📱 Интеграция

Мобильное приложение настроено для работы с этим API. Все типы данных синхронизированы между фронтендом и бекендом.
