# 📧 Пошаговая настройка Yandex SMTP

## Шаг 1: Создать пароль приложения (это главное!)

**Важно:** В Yandex доступ к SMTP работает автоматически при использовании пароля приложения. Отдельной настройки SMTP нет.

1. Откройте https://id.yandex.ru/security
2. Войдите в аккаунт `ilem1e@yandex.ru`
3. Найдите раздел **"Пароли приложений"**
4. Если пароль уже есть - **удалите его** (для безопасности)
5. Нажмите **"Создать новый пароль"**
6. Выберите приложение: **"Почта"**
7. Введите название: `CashStream Backend`
8. Нажмите **"Создать"**
9. **ВАЖНО:** Скопируйте 16-символьный пароль (показывается только один раз!)
   - Пароль будет выглядеть как: `vwviblwfryetpgjr` (16 символов без пробелов)
   - ⚠️ **Пароль начнет действовать через 2-3 часа после создания!**
   - Это нормально для Yandex - нужно подождать активации

## Шаг 2: Обновить .env файл

Откройте файл `.env` и обновите:

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ilem1e@yandex.ru
SMTP_PASS=новый-16-символьный-пароль-без-пробелов
SMTP_FROM=ilem1e@yandex.ru
```

**Важно:**
- `SMTP_PASS` - это пароль приложения (16 символов)
- Убедитесь, что в пароле **нет пробелов** (если они есть, уберите их)
- Используйте **полный email** в `SMTP_USER` и `SMTP_FROM`

## Шаг 3: Перезапустить сервер

После обновления `.env`:
1. Сервер должен автоматически перезапуститься (nodemon)
2. Или перезапустите вручную: `Ctrl+C` и `yarn dev`

## Шаг 4: Проверить работу

1. Попробуйте зарегистрировать нового пользователя
2. В логах должно появиться:
   ```
   📧 Using real SMTP server: smtp.yandex.ru (port: 587, secure: false)
   ✅ SMTP connection verified successfully
   ✅ Verification email sent: <message-id>
   ```
3. Проверьте почту получателя (и папку "Спам")

## 🔍 Проверка настроек

### Проверка 1: Пароль приложения создан
- Зайдите в https://id.yandex.ru/security
- В разделе "Пароли приложений" должен быть пароль для "Почта"

### Проверка 2: Правильность пароля в .env
- Пароль должен быть 16 символов
- Без пробелов
- Это пароль приложения, а не основной пароль аккаунта

## ❌ Частые ошибки

### Ошибка: "authentication failed"
**Причины:**
- Пароль приложения неправильный
- Доступ к SMTP не включен
- Используется основной пароль вместо пароля приложения

**Решение:**
1. Пересоздайте пароль приложения (это самое важное!)
2. Убедитесь, что используете пароль приложения, а не основной пароль
3. Обновите `.env` с новым паролем
4. Убедитесь, что пароль скопирован без пробелов

### Ошибка: "Connection timeout"
**Причины:**
- VPN блокирует подключение
- Файрвол блокирует порт 587
- Проблемы с сетью

**Решение:**
1. Попробуйте отключить VPN
2. Проверьте файрвол
3. Попробуйте порт 465 с `SMTP_SECURE=true`

### Ошибка: "This user does not have access rights"
**Причины:**
- Доступ к SMTP не включен в настройках Yandex
- Аккаунт не активирован для SMTP

**Решение:**
1. Пересоздайте пароль приложения
2. Убедитесь, что используете правильный пароль приложения (16 символов)
3. Проверьте, что пароль в `.env` без пробелов

## 📝 Альтернативные порты

Если порт 587 не работает, попробуйте:

### Порт 465 (SSL)
```env
SMTP_PORT=465
SMTP_SECURE=true
```

### Порт 25 (не рекомендуется, часто блокируется)
```env
SMTP_PORT=25
SMTP_SECURE=false
```

## ✅ Чеклист настройки

- [ ] Пароль приложения создан в https://id.yandex.ru/security
- [ ] Пароль приложения скопирован (16 символов)
- [ ] `.env` файл обновлен с новым паролем
- [ ] Пароль в `.env` без пробелов
- [ ] Сервер перезапущен
- [ ] Тестовая регистрация выполнена
- [ ] Email получен (проверена папка "Спам")

## 🆘 Если ничего не помогает

1. **Проверьте логи сервера** - там будет детальная информация об ошибке
2. **Попробуйте другой email** для тестирования
3. **Временно отключите VPN** для проверки
4. **Свяжитесь с поддержкой Yandex** - возможно, есть ограничения на аккаунте

